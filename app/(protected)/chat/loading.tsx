/**
 * Chat Route Loading State
 *
 * Displays while the chat page is loading during navigation.
 */

import { LoadingSpinner } from '@/components/ui/LoadingSpinner'

export default function ChatLoading() {
  return <LoadingSpinner />
}
