/**
 * Quiz Route Loading State
 *
 * Displays while the quiz page is loading during navigation.
 */

import { LoadingSpinner } from '@/components/ui/LoadingSpinner'

export default function QuizLoading() {
  return <LoadingSpinner />
}
