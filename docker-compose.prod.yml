services:
  # Production application
  app:
    image: ghcr.io/nicholaspsmith/loopi:latest
    container_name: loopi-app
    restart: always
    expose:
      - '3000'
    environment:
      - NODE_ENV=production
      - HOSTNAME=0.0.0.0
      - PORT=3000
      - AUTH_TRUST_HOST=true
      - DATABASE_URL=${DATABASE_URL}
      - DATABASE_HOST=postgres
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET}
      - NEXTAUTH_URL=${NEXTAUTH_URL}
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - JINA_API_KEY=${JINA_API_KEY}
      - API_KEY_ENCRYPTION_SECRET=${API_KEY_ENCRYPTION_SECRET}
      - RESEND_API_KEY=${RESEND_API_KEY}
      - SMTP_FROM=${SMTP_FROM}
      - GITHUB_TOKEN=${GITHUB_TOKEN}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - loopi-network
    volumes:
      - /opt/loopi/data/lancedb:/app/data/lancedb
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '3'

  # PostgreSQL database
  postgres:
    image: postgres:15-alpine
    container_name: loopi-postgres
    restart: always
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    expose:
      - '5432'
    volumes:
      - /opt/loopi/data/postgres:/var/lib/postgresql/data
      - /opt/loopi/backups:/backups
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER}']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - loopi-network
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '3'

  # Nginx reverse proxy with SSL
  nginx:
    image: nginx:alpine
    container_name: loopi-nginx
    restart: always
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - /opt/loopi/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - /opt/loopi/nginx/loopi.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - /opt/loopi/nginx/ssl-params.conf:/etc/nginx/ssl-params.conf:ro
    depends_on:
      - app
    networks:
      - loopi-network
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '3'

networks:
  loopi-network:
    driver: bridge
